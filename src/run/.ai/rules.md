You can only modify source code files directly specified by the user.

Do not use worktrees or alternative code versions or alternative spaces for code. Use the file system files I gave you and I will take care of git, branches, commits and versioning. Remember to work with the code in src/run and not src/generate.

If you need to modify source code files not specified by the user you need to ask for a direct permision. When asking for such permission, neet to specify why you need to modify other file. You can modify other file only when user gave you explicite permission.

You can add additonal source code files if needed, but inform me about this.

As the code is very complex, in most cases it is needed to add logging, analyze logs and only than do the code corrections, otherwise, you can do wrong moficications that will be very hard to track down afterwards and that will create a major problem for me.

When you want to add logging use MinusZero.Instance.Log.

Use only english language for writing comments.

Remeber to use meaningfull variable, attributes, functions, metods and classes identifiers/names. If the name needs to be long, is is OK.

Upper cammel case for class, metods, functions, public variables. Lower cammel case for parameter names and local variables.